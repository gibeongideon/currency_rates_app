from django.contrib.auth.models import AbstractUser


class User(AbstractUser):
    """
    :# hold user profile  information
    :# addititional fields like mobile number
     # can be added as the needs
     """
